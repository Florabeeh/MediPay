{
  "version": 2,
  "builds": [
    {
      "src": "medipay_v2/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/medipay_v2/build/$1" }
  ]
}
